# 📐 Гайд по добавлению кастомных иконок

## 🎨 Структура папок

```
bot/storage/images/
├── shop/              # Иконки для магазина
│   ├── coins.png      # 128x128px
│   ├── gems.png       # 128x128px
│   ├── hint.png       # 128x128px
│   ├── boost.png      # 128x128px
│   └── lootbox.png    # 128x128px
├── wheel/             # Колесо фортуны
│   ├── wheel-bg.png   # 600x600px (фон колеса, опционально)
│   ├── pointer.png    # 100x100px (указатель)
│   └── sectors/
│       ├── coins.png  # 64x64px
│       ├── exp.png    # 64x64px
│       └── ...
├── cosmetics/         # Косметика
│   ├── frames/
│   │   ├── fire.png       # 256x256px
│   │   ├── lightning.png  # 256x256px
│   │   ├── galaxy.png     # 256x256px
│   │   └── legend.png     # 256x256px
│   └── emoji/
│       └── ...
└── ui/                # UI элементы
    ├── back.png       # 48x48px
    ├── close.png      # 48x48px
    └── ...
```

## 📏 Размеры иконок

### Магазин (Shop)
- **Иконки товаров**: 128x128px (PNG, прозрачный фон)
- **Карточки**: 256x256px для больших превью

### Колесо фортуны (Wheel)
- **Секторы**: 64x64px (PNG, прозрачный фон)
- **Фон колеса**: 600x600px (опционально)
- **Указатель**: 100x100px (PNG, прозрачный фон)

### Косметика (Cosmetics)
- **Рамки профиля**: 256x256px (PNG, прозрачный фон)
- **Эмодзи**: 128x128px (PNG, прозрачный фон)

### UI элементы
- **Кнопки**: 48x48px
- **Иконки навигации**: 32x32px

## 🖼️ Требования к изображениям

1. **Формат**: PNG с прозрачным фоном
2. **Качество**: 72-150 DPI
3. **Оптимизация**: Сжатые через TinyPNG или аналог
4. **Размер файла**: 
   - Маленькие иконки (до 128px): < 50KB
   - Средние (128-256px): < 150KB
   - Большие (256px+): < 300KB

## 🎯 Где найти иконки

### Бесплатные ресурсы:
1. **Flaticon** - https://www.flaticon.com
   - Огромная библиотека иконок
   - Можно скачать в нужном размере
   
2. **Icons8** - https://icons8.com
   - Современный дизайн
   - Много стилей

3. **Freepik** - https://www.freepik.com
   - Векторные иконки
   - Можно изменить размер без потери качества

4. **Iconfinder** - https://www.iconfinder.com
   - Free и Premium иконки
   - Хорошая организация по категориям

### Платные (Premium):
1. **Nucleo** - https://nucleoapp.com
2. **Streamline** - https://www.streamlinehq.com

## 🔧 Как добавить иконки в проект

### 1. Создайте папки на сервере:
```bash
cd /var/www/quiz-bot/bot/storage
mkdir -p images/shop
mkdir -p images/wheel/sectors
mkdir -p images/cosmetics/frames
mkdir -p images/ui

# Установите права
sudo chown -R www-data:www-data images/
sudo chmod -R 755 images/
```

### 2. Загрузите иконки:
```bash
# Через SCP (с вашего компьютера)
scp shop-icon.png user@server:/var/www/quiz-bot/bot/storage/images/shop/

# Или через FTP клиент (FileZilla, Cyberduck)
```

### 3. Обновите код для использования иконок:

**В ShopPage.jsx:**
```jsx
// Вместо эмодзи
<div className="text-5xl mb-2">{item.icon}</div>

// Используйте изображения
<img 
  src={`/api/images/shop/${item.icon}.png`} 
  alt={item.name}
  className="w-20 h-20 object-contain mb-2"
/>
```

**В FortuneWheelPage.jsx:**
```jsx
// В SVG секторах
<image 
  href={`/api/images/wheel/sectors/${sector.type}.png`}
  x={textX - 16}
  y={textY - 16}
  width="32"
  height="32"
/>
```

### 4. Создайте endpoint для изображений:

**В `bot/public/api.php`** добавьте:
```php
// GET /images/{folder}/{filename}
case preg_match('#^/images/([a-z]+)/(.+)$#', $path, $matches) && $requestMethod === 'GET':
    $folder = $matches[1];
    $filename = basename($matches[2]);
    $filepath = __DIR__ . '/../storage/images/' . $folder . '/' . $filename;
    
    if (file_exists($filepath)) {
        $mime = mime_content_type($filepath);
        header('Content-Type: ' . $mime);
        header('Cache-Control: public, max-age=86400');
        readfile($filepath);
        exit;
    }
    
    http_response_code(404);
    exit;
```

## 🎨 Рекомендации по дизайну

### Стиль иконок:
- **Единый стиль**: Все иконки должны быть в одном стиле (flat, line, 3D)
- **Цветовая схема**: Согласованные цвета
- **Обводка**: Одинаковая толщина линий (2-3px для line style)
- **Тени**: Минимальные, для 3D эффекта

### Для темной темы:
- Используйте светлые/цветные иконки
- Добавьте легкое свечение (glow effect)
- Избегайте чисто черных иконок

### Анимации:
Добавьте CSS анимации для интерактивности:
```css
.icon-hover {
  transition: transform 0.2s;
}
.icon-hover:hover {
  transform: scale(1.1) rotate(5deg);
}
```

## 📝 Пример использования

### Магазин с кастомными иконками:
```jsx
const itemIcons = {
  hint: '/api/images/shop/hint.png',
  boost: '/api/images/shop/boost.png',
  lootbox: '/api/images/shop/lootbox.png',
}

<img 
  src={itemIcons[item.type] || item.icon}
  alt={item.name}
  className="w-20 h-20 object-contain drop-shadow-lg"
  onError={(e) => {
    // Fallback на эмодзи если картинка не загрузилась
    e.target.style.display = 'none'
    e.target.nextSibling.style.display = 'block'
  }}
/>
<div className="text-5xl hidden">{item.icon}</div>
```

## 🚀 Оптимизация

1. **Lazy loading** для изображений:
```jsx
<img loading="lazy" src="..." />
```

2. **WebP формат** для лучшего сжатия:
```bash
# Конвертация PNG в WebP
cwebp -q 80 input.png -o output.webp
```

3. **Sprites** для маленьких иконок:
Объедините все маленькие иконки в один файл

---

**Готовы начать?** 
Загрузите первые иконки и протестируйте! 🎨

