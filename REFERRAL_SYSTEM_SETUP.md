# 🎁 Инструкция по запуску реферальной системы

## ✅ Что было реализовано

### 1. База данных
- ✅ Миграция `20241229_000001_create_referrals_system.php`
- ✅ Таблицы: `referrals`, `referral_milestones`, `user_referral_milestones`
- ✅ Обновлена таблица `user_profiles` с полями реферальной системы

### 2. Модели
- ✅ `Referral` - модель реферала
- ✅ `ReferralMilestone` - модель milestone наград
- ✅ Обновлены `User` и `UserProfile` с новыми связями

### 3. Сервисы
- ✅ `ReferralService` - основная логика реферальной системы
  - Генерация уникальных кодов
  - Применение кодов
  - Активация рефералов
  - Выдача наград
  - Milestone система

### 4. Обработчики команд
- ✅ Команда `/referral` или `/invite` - информация о реферальной программе
- ✅ Обработка реферального кода в `/start ref_CODE`
- ✅ Callback handlers для действий с рефералами

### 5. Интеграция
- ✅ Автоматическая проверка активации после игр
- ✅ Уведомления пользователям
- ✅ Milestone награды

---

## 🚀 Шаги для запуска

### Шаг 1: Применить миграцию

```bash
cd /Users/evgeny/Desktop/Social/bot
composer migrate
```

### Шаг 2: Заполнить milestone награды

```bash
cd /Users/evgeny/Desktop/Social/bot
composer seed
```

Или только milestone:
```bash
php bin/seed.php
```

### Шаг 3: Настроить переменные окружения

Добавь в `bot/config/app.env`:

```env
# Имя бота для генерации реферальных ссылок
TELEGRAM_BOT_USERNAME=your_bot_username
```

### Шаг 4: Перезапустить бота

Если бот работает на сервере:
```bash
# Перезагрузить PHP-FPM или веб-сервер
sudo systemctl restart php-fpm
sudo systemctl restart nginx
```

---

## 📋 Milestone награды

| Количество рефералов | Награда | Монеты | Опыт |
|---------------------|---------|--------|------|
| 1 | 🌟 Первый друг | 50 | 25 |
| 5 | 🎯 Наставник | 200 | 100 |
| 10 | 🏅 Рекрутер | 500 | 250 |
| 25 | 👑 Король рефералов | 1500 | 750 |
| 50 | ⭐ Легенда | 5000 | 2500 |
| 100 | 🌌 Мастер вселенной | 15000 | 7500 |

---

## 🎮 Как работает система

### Для реферера (приглашающего):
1. Получает уникальный код через `/referral`
2. Делится ссылкой с друзьями
3. Когда друг регистрируется по ссылке, реферер получает уведомление
4. Когда друг сыграет **3 игры**, реферер получает:
   - 💰 **100 монет**
   - ⭐ **50 опыта**
5. За количество рефералов получает milestone награды

### Для реферала (приглашенного):
1. Переходит по ссылке или вводит код при `/start ref_CODE`
2. Сразу получает:
   - 💰 **50 монет**
   - ⭐ **25 опыта**
3. После 3 игр активируется и дает награду другу

---

## 🔧 Команды для пользователей

### `/referral` или `/invite`
Показывает:
- Реферальный код
- Статистику приглашенных
- Прогресс до следующей награды
- Кнопки для шаринга

### `/start ref_XXXXXX`
Применяет реферальный код при первом запуске

---

## 🧪 Тестирование

### 1. Создать реферальный код
```
/referral
```

### 2. Проверить ссылку
Скопируй ссылку из сообщения и открой в другом аккаунте

### 3. Проверить активацию
Сыграй 3 игры любым способом:
- Дуэль
- Правда или ложь
- Категории

### 4. Проверить награды
```
/profile - проверить монеты и опыт
/referral - проверить статус реферала
```

---

## 📊 Мониторинг

### Проверка рефералов в БД:

```sql
-- Все рефералы
SELECT * FROM referrals ORDER BY created_at DESC LIMIT 10;

-- Активные рефералы
SELECT * FROM referrals WHERE status = 'active';

-- Статистика по пользователям
SELECT 
    u.first_name,
    up.referral_code,
    up.total_referrals,
    COUNT(r.id) as total_refs
FROM users u
JOIN user_profiles up ON u.id = up.user_id
LEFT JOIN referrals r ON u.id = r.referrer_user_id
GROUP BY u.id
ORDER BY up.total_referrals DESC
LIMIT 10;

-- Milestone награды
SELECT * FROM referral_milestones ORDER BY referrals_count;
```

---

## 🐛 Troubleshooting

### Проблема: Реферальный код не генерируется
**Решение:** Убедись, что миграция применена:
```bash
cd bot && composer migrate
```

### Проблема: Награды не начисляются
**Решение:** Проверь логи:
```bash
tail -f bot/storage/logs/app.log | grep -i referral
```

### Проблема: Реферал не активируется
**Решение:** Проверь количество игр:
```sql
SELECT 
    u.first_name,
    r.referred_games_played,
    r.status
FROM referrals r
JOIN users u ON r.referred_user_id = u.id
WHERE r.status = 'pending';
```

---

## 🔄 Настройка параметров

Если нужно изменить награды или условия активации, отредактируй константы в:

**`bot/src/Application/Services/ReferralService.php`**

```php
private const BASE_REFERRER_REWARD_COINS = 100;      // Награда рефереру
private const BASE_REFERRER_REWARD_EXP = 50;
private const BASE_REFERRED_REWARD_COINS = 50;       // Награда приглашенному
private const BASE_REFERRED_REWARD_EXP = 25;
private const ACTIVATION_REQUIRED_GAMES = 3;         // Необходимо игр для активации
```

---

## 📈 Аналитика

### Ключевые метрики для отслеживания:

1. **Conversion Rate** = (Использованные коды / Созданные коды) × 100%
2. **Activation Rate** = (Активные рефералы / Зарегистрированные) × 100%
3. **K-factor** = Среднее количество рефералов на пользователя

### Запросы для аналитики:

```sql
-- Conversion Rate
SELECT 
    COUNT(DISTINCT referral_code) as total_codes,
    COUNT(DISTINCT CASE WHEN referred_user_id IS NOT NULL THEN referral_code END) as used_codes,
    ROUND(COUNT(DISTINCT CASE WHEN referred_user_id IS NOT NULL THEN referral_code END) * 100.0 / COUNT(DISTINCT referral_code), 2) as conversion_rate
FROM (
    SELECT referral_code FROM user_profiles WHERE referral_code IS NOT NULL
    UNION ALL
    SELECT referral_code FROM referrals
) codes;

-- Activation Rate
SELECT 
    COUNT(*) as total_referrals,
    COUNT(CASE WHEN status = 'active' THEN 1 END) as active_referrals,
    ROUND(COUNT(CASE WHEN status = 'active' THEN 1 END) * 100.0 / COUNT(*), 2) as activation_rate
FROM referrals;

-- Top рефереры
SELECT 
    u.first_name,
    u.username,
    up.total_referrals,
    COUNT(r.id) as active_refs
FROM users u
JOIN user_profiles up ON u.id = up.user_id
LEFT JOIN referrals r ON u.id = r.referrer_user_id AND r.status = 'active'
WHERE up.total_referrals > 0
GROUP BY u.id
ORDER BY up.total_referrals DESC
LIMIT 20;
```

---

## ✨ Дальнейшие улучшения

Что можно добавить в будущем:

1. **Временные акции** - удвоенные награды за рефералов в выходные
2. **Реферальные конкурсы** - топ-3 реферера месяца получают призы
3. **Цепочки рефералов** - награды за рефералов 2-го уровня
4. **Персональные промокоды** - влиятельные игроки получают кастомные коды
5. **API для Mini App** - красивая страница рефералов в веб-приложении
6. **Push-уведомления** - уведомление рефереру когда его друг активировался

---

## 🎉 Готово!

Реферальная система полностью готова к работе!

Для проверки работы выполни команду:
```bash
/referral
```

**Примечание:** Не забудь установить `TELEGRAM_BOT_USERNAME` в `app.env` для корректной генерации ссылок!

